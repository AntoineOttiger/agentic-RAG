# Noyau (suffit pour le smoke run en backend mémoire)
pydantic>=2.5
pyyaml>=6.0
python-dotenv>=1.0

# Backend réel — décommenter / installer via `pip install -e .[real]`
# sentence-transformers>=2.3
# qdrant-client>=1.7
# llama-index-core>=0.10
# mistralai>=1.0

# Éval avancée (optionnel) — `pip install -e .[ragas]`
# ragas>=0.1
# datasets>=2.14
