# Copie ce fichier en `.env` (non versionné) et renseigne ta clé.
#   cp .env.example .env      (ou copie manuelle sous Windows)
# Le nom de la variable est celui référencé par les configs (api_key_env).

MISTRAL_API_KEY=
